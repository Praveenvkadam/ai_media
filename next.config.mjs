/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: ['unsplash.com', 'imagekit.io', 'clerk.com'],
  },
  reactCompiler: true,
};

export default nextConfig;
